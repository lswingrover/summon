// AppVersion.swift — single source of truth for version string
// Updated atomically by ship_summon.py on every release.
enum AppVersion {
    static let current = "1.1.2"
    static let build   = "4"
}
